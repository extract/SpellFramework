namespace SpellFramework
{
    class JsonReader
    {
        //public static T LoadFile<T>(string location)
        //{

        //}

        /*public static T GetFileType<T>()
        {
            switch (typeof(T).)
            {
                case Object:
                    break;
                default:
                    break;
            }
            return;
        }*/
    }
}
